import java.io.*;
import java.util.*;

//a and b are sorted
//smaller numbers into smaller indices

public class mergeSort{
    
    public int[] merge(int[] a, int[] b){
	int[] result = new int[a.length + b.length];
	for (int i=0; i<max(a.length, b.length)){
	    //check if a or b is empty
	    

}